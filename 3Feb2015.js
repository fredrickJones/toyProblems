'use strict';
// create a function that takes in 3 strings as parameters. the goal is to swap all x characters in the string for y characters.
var swapper = function(str, char1, char2) {
	var stringArr = str.split('');
	for (var i = 0; i < stringArr.length; i++) {
		if (stringArr[i] === char1) {
			stringArr[i] === char2;
		}
		var string = arr.join('');
		return string;
	};
};